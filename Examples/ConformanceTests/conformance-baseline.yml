# Expected conformance test failures.
# The conformance runner treats these as known issues (exit 0).
# If a test listed here starts passing, the runner fails (update the baseline).
#
# The scripts filter via `--spec-version 2025-11-25`, which excludes
# scenarios whose `specVersions` list does not include that version (for
# example, `['2025-03-26']`-only backcompat scenarios and `['extension']`
# scenarios). Those do not need to be listed here.

client: []
